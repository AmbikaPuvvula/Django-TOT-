from django.contrib import admin
from CrudApp.models import Student, Emp, StudentDetails

# Register your models here.
admin.site.register(Student)
admin.site.register(Emp)
admin.site.register(StudentDetails)
